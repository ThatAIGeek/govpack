from .apis import *
from .create_var import *

med_api0 = api+list(med_dict.values())[0]
med_api1 = api+list(med_dict.values())[1]
med_api2 = api+list(med_dict.values())[2]

car_api0 = api+list(car_dict.values())[0]
